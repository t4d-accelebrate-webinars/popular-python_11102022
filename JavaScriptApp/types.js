let message = "Hello";
console.log(typeof message);

// Dynamic Typing (the type of the variable can change during execution)
// message = 2;
// console.log(typeof message);

// Loose Typing (automatic type coercion)
// message = message + 2
// console.log(message);

// Loose Typing (automatic type coercion)
// console.log([] + {});

// JavaScript is very loosely typed, it will coerce almost anything
